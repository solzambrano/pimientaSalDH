<!DOCTYPE html>
<html>
  <head>
    <title>Pimienta & sal</title>
     <link rel="stylesheet" href="/css/menu.css" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body>
    <!-- Navbar (sit on top) -->
    <%- include('./partials/nav')%>

    <!-- Page content -->
      <!-- Menu Section -->
      <section class="" id="menu">
          <h1 class="">Nuestro Menu</h1>
        <article class="menuContent">
          <% for (let item of menu) {%>
          <article class="card">
            <h4 id="title-menu"><%= item.nombre %></h4>
            <div class="container-picture">
            <a href="/menu/detail/<%= item.id %>"><img src="<%=item.image %>"/></a>
            </div>
            <p class="ps-text-grey"><%= item.descrpcion%> </p>
            <p>precio: <%=item.precio%></p>
          </article>
        <%}%>
        </article>
      </section>
      <!-- Footer -->
      <%-include('./partials/footer')%>
  </body>
</html>
