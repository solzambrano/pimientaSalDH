<!DOCTYPE html>
<html>
  <head>
    <title>Pimienta & sal</title>
     <link rel="stylesheet" href="/css/index.css" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body>
    <!-- Navbar (sit on top) -->
    <%- include('./partials/nav')%>
   

    <!-- Header -->
    <%- include('./partials/header')%>

    <!-- Page content -->
      <!-- Menu Section -->
      <section class="" id="menu">
          <h1 class="">Nuestro Menu</h1>
        <article class="menuContent">
          <% for (let item of menuCasa) {%>
          <article class="card">
            <h4 id="title-menu"><%= item.nombre %></h4>
            <div class="container-picture">
              <img src="<%=item.image %>"/>
            </div>
            <p class="ps-text-grey"><%= item.descrpcion%> </p>
            <p>precio: <%=item.precio%></p>
          </article>

        <%}%>
        </article>
      </section>
      <h3 id="text-y-mas"><a href="./menu">.... y MÁS </a></h3>
      <!-- Footer -->
      <%-include('./partials/footer')%>
  </body>
</html>
